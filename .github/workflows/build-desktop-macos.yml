name: Build Desktop — macOS

on:
  push:
    tags:
      - "desktop-v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch or tag to build"
        default: "main"

jobs:
  build-mac:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Get pnpm store directory
        id: pnpm-cache
        run: echo "STORE_PATH=$(pnpm store path)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared packages
        run: |
          pnpm --filter @workspace/shared build
          pnpm --filter @workspace/db build

      - name: Build desktop app
        run: pnpm --filter @workspace/needsops-desktop build

      - name: Package — macOS arm64 + x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false  # disable code signing for dev builds
        run: pnpm --filter @workspace/needsops-desktop dist:mac

      - name: Upload DMG artifacts
        uses: actions/upload-artifact@v4
        with:
          name: needsops-desktop-macos
          path: artifacts/needsops-desktop/release/**/*.dmg
          if-no-files-found: warn
          retention-days: 30

      - name: Create GitHub Release (on tag push)
        if: startsWith(github.ref, 'refs/tags/desktop-v')
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/needsops-desktop/release/**/*.dmg
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
