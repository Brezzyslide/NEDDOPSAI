OPENCLAW SPAWN-MODE DISCOVERY CORRECTION

We have completed the real Mac operator proof up to the broker.

Proven:

Desktop Connector builds successfully.
Broker starts on 127.0.0.1:19002.
Broker health reports:
adapter: live
ok: true
detail: "spawn mode; bin=openclaw"
NeedsOps OpenClaw CLI is authenticated to OpenAI API and successfully returns NEEDSOPS_OPENAI_OK.
Authenticated POST /v1/evidence/discover works end-to-end at the broker layer.
However, it returns the synthetic connectivity candidate.

Root cause discovered during live proof:

The implementation assumes live evidence discovery can call:

POST http://127.0.0.1:19001/agent/discover

But this Mac is using:

OPENCLAW_LIVE_MODE=spawn

In spawn mode there is no persistent OpenClaw HTTP discovery service on port 19001. Direct probes to /agent/discover return no response because that endpoint belongs to the bridge-http assumption, not the actual spawn-mode runtime.

Required correction:

Implement real evidence discovery for the existing spawn-mode adapter.

Reuse the same proven OpenClaw CLI execution mechanism already used by the runtime.

For an evidence discovery request, spawn OpenClaw with the configured OPENCLAW_BIN_PATH/openclaw binary and invoke the main NeedsOps agent with a tightly bounded discovery prompt.

The prompt must include:

organisationId
executionId
specialistCode
searchObjective
unresolvedReferences
allowedDiscoveryScope
allowExternalWebSearch
maxHops
maxSources
maxPassages

OpenClaw must be instructed:

Discovery only. Locate/read evidence and return structured candidate records. Do not perform final professional analysis.

Require machine-readable JSON that maps to the existing BrokerCandidateEvidence / CandidateEvidence contract.

Validate and parse the JSON server-side.

Do not fabricate or synthesize a candidate when the real OpenClaw call fails. Return:

openClawStatus: "unavailable" or a typed discovery error with candidates: [].

Keep bridge-http support separately if required:

spawn → CLI-based discovery
bridge-http → HTTP discovery endpoint, only if the actual OpenClaw bridge supports one

Do not assume /agent/discover exists without proving it.

Add tests proving:

spawn discovery invokes real OpenClaw adapter;
valid structured response maps to CandidateEvidence[];
malformed response returns no trusted candidates;
timeout fails safely;
synthetic connectivity fixture is never returned in live mode;
simulated mode remains clearly labelled;
Authority Gate remains downstream and unchanged.

After implementation, give me one exact curl command to rerun against:

POST http://127.0.0.1:19002/v1/evidence/discover

Success criteria:

The returned candidate must come from the real OpenClaw run and must not contain:

retrievalMethod: "connectivity_test"

or

Synthetic candidate generated by broker.

Stop after that correction. Do not start Cloudflare integration yet.